# ── Core Framework ────────────────────────────────────────────────────────────
Django==5.2.14
asgiref==3.11.1
sqlparse==0.5.5

# ── Database ──────────────────────────────────────────────────────────────────
psycopg2-binary==2.9.12        # Task 1: PostgreSQL driver (dev/prod uchun binary)

# ── Environment ───────────────────────────────────────────────────────────────
python-dotenv==1.2.2           # Task 1: .env fayl o'qish

# ── Async Workers ─────────────────────────────────────────────────────────────
celery==5.4.0                  # Task 6, 7, 9: AI diagnostika, billing, notifications
django-celery-beat==2.8.1      # Task 9: Cron jobs (streak, league reset) — Django 5.2 support
django-celery-results==2.6.0   # Task 6: Worker natijalarini DB da saqlash — Django 5.2 support

# ── Cache & Session ───────────────────────────────────────────────────────────
redis==5.2.1                   # Task 2, 5: Device fingerprinting, leaderboard
django-redis==5.4.0            # Task 2, 5: Django cache backend for Redis
# ISSUE-102: distributed lock — core/locks.py SET NX EX pattern, dep'siz

# ── Real-time ─────────────────────────────────────────────────────────────────
channels==4.2.0                # Task 4: WebSocket (heartbeat, leaderboard live)
channels-redis==4.2.1          # Task 4: Channel layer for Redis
daphne==4.2.1                  # Task 4: ASGI server (channels.testing ham talab qiladi)

# ── Auth ──────────────────────────────────────────────────────────────────────
django-allauth==65.3.0         # Task 2: Google OAuth, email auth
PyJWT==2.10.1                  # Task 2: JWT token generation/verification

# ── Telegram ──────────────────────────────────────────────────────────────────
python-telegram-bot==21.11.1   # Task 2, 9: TMA auth, notifications bot

# ── REST API ──────────────────────────────────────────────────────────────────
djangorestframework==3.15.2    # Task 2, 4: TMA va mobile uchun API endpoints
django-cors-headers==4.7.0     # Task 2: CORS (TMA, mobile app)
drf-spectacular==0.27.2        # ISSUE-201: OpenAPI 3.0 schema + Swagger UI + Redoc
django-fsm-2==4.0.0            # ISSUE-205: state machine field decorators for status models
django-flags==5.2.0             # ISSUE-505: feature flag toggling (per-user/per-org/percentage)

# ── Frontend Helpers ──────────────────────────────────────────────────────────
django-htmx==1.21.0            # Task 1+: HTMX request detection middleware

# ── File & Media ──────────────────────────────────────────────────────────────
Pillow==11.2.1                 # Task 3: Rasm ishlash, watermark
openpyxl==3.1.5                # Task 3: Excel/CSV bulk import parser
python-docx==1.1.2             # Task 3: Word (.docx) bulk import parser
pypdf==6.11.0                  # Task 3: PDF bulk import parser (text-based PDFs)

# ── Search ────────────────────────────────────────────────────────────────────
meilisearch==0.32.0            # Task 9: Typo-tolerant instant search

# ── AI / LLM ──────────────────────────────────────────────────────────────────
anthropic==0.102.0             # Task 3, 6: AI parser (bulk import) + AI tutor feedback

# ── HTTP Client ───────────────────────────────────────────────────────────────
httpx==0.28.1                  # Task 2, 7: Async HTTP (Payme, Click, Telegram)
requests==2.32.3               # Task 2, 7: allauth Google OAuth + Payme/Click
cryptography==44.0.2           # Task 2: allauth Google JWT verification

# ── Observability (Task 10) ───────────────────────────────────────────────────
django-prometheus==2.4.1       # /metrics endpoint (Prometheus exporter)
python-json-logger==3.3.0      # Structured JSON logging (prod)

# ── Audit / Compliance (ISSUE-401) ────────────────────────────────────────────
# SOC2/ISO 27001: kim qachon nima o'zgartirgan? — historical_<model> shadow tables
django-simple-history>=3.4     # ISSUE-401 SOC2 audit trail

# ── Static Files (gunicorn/daphne — nginx-siz serve qilish) ───────────────────
# Middleware base.py'da ulanadi → barcha env'larda (test/dev/prod) kerak.
whitenoise==6.9.0
