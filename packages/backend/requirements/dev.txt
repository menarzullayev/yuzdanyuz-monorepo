-r base.txt

# ── Debug & Profiling ─────────────────────────────────────────────────────────
django-debug-toolbar==5.2.0    # SQL query va performance nazorat

# ── Testing ───────────────────────────────────────────────────────────────────
pytest==8.3.5
pytest-django==4.10.0
pytest-asyncio==0.26.0         # Async view va channel testlari
factory-boy==3.3.3             # Test fixture (User, Org, Question factory)
coverage==7.8.0                # Test qamrovi hisoboti

# ── Code Quality ──────────────────────────────────────────────────────────────
ruff==0.11.8                   # Linter + formatter (flake8 + isort o'rniga)

# ── Dev Utilities ─────────────────────────────────────────────────────────────
ipython==8.35.0                # Django shell uchun
django-extensions==3.2.3       # shell_plus, show_urls, graph_models
