-r base.txt

# ── Debug & Profiling ─────────────────────────────────────────────────────────
django-debug-toolbar==6.3.0    # SQL query va performance nazorat

# ── Testing ───────────────────────────────────────────────────────────────────
pytest==8.3.5
pytest-django==4.10.0
pytest-asyncio==0.26.0         # Async view va channel testlari
pytest-cov==6.0.0              # Coverage reporting plugin
factory-boy==3.3.3             # Test fixture (User, Org, Question factory)
coverage==7.8.0                # Test qamrovi hisoboti
fakeredis==2.35.1              # Redis mock for tests (conftest mock_redis fixture)

# ── Code Quality ──────────────────────────────────────────────────────────────
ruff==0.15.13                  # Linter + formatter (flake8 + isort o'rniga)
pre-commit==4.6.0              # Git hooks framework (commit + push validation)

# ── Dev Utilities ─────────────────────────────────────────────────────────────
ipython==8.35.0                # Django shell uchun
django-extensions==4.1       # shell_plus, show_urls, graph_models
