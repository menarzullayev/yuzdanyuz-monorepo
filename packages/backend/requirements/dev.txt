-r base.txt

# ── Debug & Profiling ─────────────────────────────────────────────────────────
django-debug-toolbar==5.2.0    # SQL query va performance nazorat

# ── Testing ───────────────────────────────────────────────────────────────────
pytest==9.0.3
pytest-django==4.12.0
pytest-asyncio==1.3.0         # Async view va channel testlari
factory-boy==3.3.3             # Test fixture (User, Org, Question factory)
coverage==7.14.0                # Test qamrovi hisoboti

# ── Code Quality ──────────────────────────────────────────────────────────────
ruff==0.15.13                  # Linter + formatter (flake8 + isort o'rniga)
pre-commit==4.6.0              # Git hooks framework (commit + push validation)

# ── Dev Utilities ─────────────────────────────────────────────────────────────
ipython==8.35.0                # Django shell uchun
django-extensions==3.2.3       # shell_plus, show_urls, graph_models
